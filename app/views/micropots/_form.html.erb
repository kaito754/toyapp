<%= form_with(model: micropot, local: true) do |form| %>
  <% if micropot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(micropot.errors.count, "error") %> prohibited this micropot from being saved:</h2>

      <ul>
      <% micropot.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :micropot_content %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :micropot_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
